import { Layout } from  '../../Layout'

export function Students () {
  /* Features from old FrontEnd
    - table is paginated to 8 items
    - on key up in searchfield, table is filtered to show users by search value
    - table has Navn and Klasse
  */
  return (
    <Layout>
      Students
    </Layout>
  )
}
